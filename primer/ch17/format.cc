#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std;

int main(int argc, char const *argv[])
{
    /*
    char ch;
    while(cin.get(ch))
        cout.put(ch);
    */

    int ch;
    while((ch = cin.get())!=EOF)
        cout.put(ch);


    return 0;
}
